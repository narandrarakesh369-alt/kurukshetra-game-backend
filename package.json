{
  "name": "kurukshetra-game",
  "version": "1.0.0",
  "description": "Kurukshetra Card War Arena",
  "scripts": {
    "install": "cd client && npm install --no-package-lock && npm run build && cd ../server && npm install",
    "start": "cd server && node index.js"
  }
}
