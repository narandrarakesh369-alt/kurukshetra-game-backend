{
  "name": "kurukshetra-server-root",
  "version": "1.0.0",
  "description": "Root package for Render deployment",
  "scripts": {
    "install": "cd server && npm install",
    "start": "cd server && node index.js"
  }
}
